# تشغيل SupportFlow على Windows من الصفر

1. فكّي ملف ZIP بالكامل. ادخلي فولدر `supportflow` اللي فيه `README.md`. **ما تستخدميش فولدر النسخة القديمة.**
2. شغّلي `1_SETUP_WINDOWS.cmd` مرة واحدة واستني رسالة `Setup complete`. محتاج Python 3.12 واتصال إنترنت لأول تثبيت فقط. ملف `.env` هيتعمل تلقائيًا؛ ما تشاركيهوش.
3. شغّلي `2_START_BACKEND.cmd` **مرة واحدة** وسيبي النافذة مفتوحة لحد ما يظهر `Application startup complete`. لو في باك إند قديم شغال، اقفليه بـ Ctrl+C قبل تشغيل الجديد عشان قفل Qdrant.
4. شغّلي `3_START_FRONTEND.cmd` وسيبي نافذته مفتوحة. الواجهة جاهزة داخل ZIP؛ **مفيش npm أو Rollup مطلوبين على Windows.**
5. شغّلي `4_GET_TOKEN.cmd` والصقي السطر الطويل في خانة التوكن على `http://127.0.0.1:5173`. التوكن صلاحيته ساعة؛ شغّلي نفس الملف لإصدار واحد جديد وقت الحاجة.

- اختبار API: `http://127.0.0.1:8000/health`.
- لو ظهر `Failed to fetch`: تأكدي نافذة backend ما زالت شغالة وبيفتح `/health`.
- لو ظهر `Storage folder ... already accessed`: في نسختين من الـbackend شغّالين؛ اقفلي القديمة وشغّلي واحدة فقط.
- الملف ده مشروع محلي قابل للتجربة. متطلبات Lovable والنشر وLangfuse البعيد والتقييمات الحية والفيديو موضحة في README، وما نعتبرهاش منفّذة بمجرد تشغيله محليًا.
